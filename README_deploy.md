# Deployment con Docker Compose (locale)

## Requisiti
- Docker e Docker Compose installati.

## Avvio rapido
1. Copia l'esempio env:
   ```bash
   cp .env.example .env
   # modifica .env se vuoi cambiare JWT_SECRET
